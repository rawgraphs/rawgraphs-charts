# Run developer sandbox (with examples)

```sh
npm run sandbox
```

# Create a new example / configuration

1. Open directory `example/configurations`
2. Duplicate one of the existing configurations
3. Rename it and tweak it
4. Restart the sandbox